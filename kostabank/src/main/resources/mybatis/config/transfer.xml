<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="transfer">

	<select id="checkBal" parameterType="string" resultType="Integer" >
		select a.balance from kangbank_account a,kangbank_customer c
		where a.email=c.email and accountNo=#{value}
	</select>
	
	<update id="deposit" parameterType="transferVO">
		update kangbank_account 
		set balance=balance+#{balance}
		where accountNo=#{accountNo} and accountPass=#{accountPass}
	</update>
	
	<update id="withdraw" parameterType="transferVO">
		update kangbank_account 
		set balance=balance-#{balance}
		where accountNo=#{accountNo} and accountPass=#{accountPass}
	</update>

	
</mapper>